# Rust build artifacts
rust/packet_engine/target/

# Compiled libraries (rebuilt from source via ./scripts/build-universal.sh)
sentinel/Frameworks/*.a
sentinel/Frameworks/ios-arm64/
sentinel/Frameworks/ios-arm64-simulator/

# Generated header (rebuilt from source)
sentinel/PacketTunnelExtension/Bridge/packet_engine.h

# Xcode
*.xcuserdata
DerivedData/
*.xcuserstate

# macOS
.DS_Store


# Claude Code local settings
.claude/settings.local.json

# Environment
.env
.env.local

# Protobuf definitions (internal — not for public repo)
*.proto

# Markdown docs (public repo — keep strategic/internal docs out)
*.md
!README.md
